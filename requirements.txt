# Health Data Analytics System Requirements
# Core dependencies for database and data processing

# No external dependencies required - using only Python standard library:
# - sqlite3 (built-in)
# - csv (built-in)
# - pathlib (built-in)
# - logging (built-in)
# - datetime (built-in)
# - typing (built-in)

# Development dependencies for code quality:
pre-commit>=3.6.0

# Testing dependencies:
pytest>=7.0.0
pytest-cov>=4.0.0

# Data analysis and visualization dependencies:
pandas>=1.5.0
matplotlib>=3.6.0
seaborn>=0.11.0
numpy>=1.21.0
jupyter>=1.0.0
ipykernel>=6.0.0

# Web UI dependencies:
streamlit>=1.28.0
plotly>=5.15.0